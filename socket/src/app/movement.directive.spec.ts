import { MovementDirective } from './movement.directive';

describe('MovementDirective', () => {
  it('should create an instance', () => {
    const directive = new MovementDirective();
    expect(directive).toBeTruthy();
  });
});
